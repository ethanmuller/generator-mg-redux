language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-gulp-mg' ]; then cd .. && eval "mv $currentfolder generator-gulp-mg" && cd generator-gulp-mg; fi

